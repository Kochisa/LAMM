<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <RootNamespace>LocalAIModelManager.ControlHelper</RootNamespace>
    <AssemblyName>LAMM.ControlHelper</AssemblyName>
    <!-- Console subsystem: it needs a console it can attach to the engine's. -->
    <DisableWinExeOutputInference>true</DisableWinExeOutputInference>
  </PropertyGroup>

</Project>
